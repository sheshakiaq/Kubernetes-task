apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: task-kube
spec:
  type: NodePort
  selector:
    app: testing
  ports:
    - name: http
      protocol: TCP
      port: 80
      nodePort: 30081
      targetPort: 80
